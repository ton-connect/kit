---
description: Basic settings for Cursor
globs:
alwaysApply: true
---

When you're generating many updates for the same file, do that as single change in one diff.
Use pnpm
Apply `pnpm lint:fix` in the !root!(do not apply any `cd` commands) directory before trying to fix lint issues.
Keep context as short as possible, do not overcomplicate it
